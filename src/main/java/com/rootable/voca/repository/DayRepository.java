package com.rootable.voca.repository;

import com.rootable.voca.domain.Day;
import org.springframework.data.jpa.repository.JpaRepository;

public interface DayRepository extends JpaRepository<Long, Day> {
}
